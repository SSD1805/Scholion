{
  "name": "scholion-mermaid-docs",
  "private": true,
  "version": "0.0.0",
  "description": "Pinned renderer for reproducible Scholion documentation SVGs.",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "render": "node render.mjs --write",
    "check": "node render.mjs --check",
    "migrate-inline": "node render.mjs --migrate-inline"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "11.16.0",
    "puppeteer": "25.0.0"
  }
}
